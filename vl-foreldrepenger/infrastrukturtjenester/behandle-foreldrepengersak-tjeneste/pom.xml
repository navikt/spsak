<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>no.nav.foreldrepenger</groupId>
		<artifactId>infrastrukturtjenester-pom</artifactId>
        <version>${revision}${sha1}${changelist}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>behandle-foreldrepengersak-tjeneste</artifactId>
    <packaging>jar</packaging>
    <name>FPSAK :: Infrastrukturtjenester - Behandle Foreldrepengersak</name>

    <dependencies>
        <!-- Domenetjenester -->
        <dependency>
            <groupId>no.nav.foreldrepenger</groupId>
            <artifactId>person</artifactId>
        </dependency>
        <dependency>
            <groupId>no.nav.foreldrepenger</groupId>
            <artifactId>behandling</artifactId>
        </dependency>

        <!-- Kontrakter -->
        <dependency>
            <groupId>no.nav.tjenester</groupId>
            <artifactId>nav-behandleForeldrepengesak-v1-tjenestespesifikasjon</artifactId>
            <classifier>jaxws</classifier>
        </dependency>

        <!-- Prosjekt -->
        <dependency>
            <groupId>no.nav.vedtak.felles</groupId>
            <artifactId>felles-bom</artifactId>
        </dependency>

        <!-- Integrasjon -->
        <dependency>
            <groupId>no.nav.vedtak.felles.integrasjon</groupId>
            <artifactId>behandlesak-klient</artifactId>
        </dependency>
        <dependency>
            <groupId>no.nav.vedtak.felles.integrasjon</groupId>
            <artifactId>sak-klient</artifactId>
        </dependency>
        
        <!-- Infrastrukturtjenester -->
        <dependency>
            <groupId>no.nav.vedtak.felles.sikkerhet</groupId>
            <artifactId>felles-sikkerhet</artifactId>
        </dependency>

        <!-- Test avhengigheter -->
        <dependency>
            <groupId>no.nav.vedtak.felles</groupId>
            <artifactId>felles-testutilities</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>no.nav.foreldrepenger</groupId>
            <artifactId>behandlingslager-testutilities</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>no.nav.foreldrepenger</groupId>
            <artifactId>datavarehus-tjeneste</artifactId>
        </dependency>
		
		<dependency>
			<!-- for Java 10-->
			<groupId>javax.jws</groupId>
			<artifactId>jsr181-api</artifactId>
		</dependency>
		
    </dependencies>


</project>
