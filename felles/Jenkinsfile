@Library('deploy')
import deploy

def deployLib = new deploy()

def call(body) {
    try {
		
        stage ('Build') {
			sh 'mvn -s ../mvn-settings.xml install -DskipTests' 
        }
		
	} catch (error) {
		throw error
	}
}

return this;